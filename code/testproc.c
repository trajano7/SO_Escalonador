#include <limits.h>
#include <stdio.h>

int main(int argc, char *argv[]) {
  // printf("Filename: %s\n", argv[0]);

  // printf("Inside %s!\n", argv[0]);
  printf("PROCESSO FILHO\n");

  for (unsigned int i = 0; i < INT_MAX; i++) {
  }
  for (unsigned int i = 0; i < INT_MAX; i++) {
  }
  for (unsigned int i = 0; i < INT_MAX; i++) {
  }
  for (unsigned int i = 0; i < INT_MAX; i++) {
  }
  for (unsigned int i = 0; i < INT_MAX; i++) {
  }
  for (unsigned int i = 0; i < INT_MAX; i++) {
  }

  return 0;
}
